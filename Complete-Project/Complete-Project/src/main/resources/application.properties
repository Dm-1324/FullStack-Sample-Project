spring.application.name=Complete-Project
spring.datasource.url=jdbc:mysql://localhost:3306/student
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# CORS Configuration (Important for Vue.js to talk to the backend)
# Allows requests from the Vue.js development server (usually port 3000 or 8081/8082)
# Adjust the port as needed for your Vue setup
app.cors.allowed-origins=http://localhost:8080,http://localhost:8081,http://localhost:5173